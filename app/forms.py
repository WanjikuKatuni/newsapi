# from flask_wtf import FlaskForm #flaskform helps create a form class
# from wtforms import StringField,TextAreaField,SubmitField #help to create a label, text are and submit button for the form.
# from wtforms.validators import DataRequired #validator that prevents the form from beign submited while empty


# class ReviewForm(FlaskForm): #review form class that inherits the flask form class

#     title = StringField('Review title', validators= [DataRequired()])
#     review = TextAreaField('Article review', validators=[DataRequired()])
#     submit = SubmitField('Submit')